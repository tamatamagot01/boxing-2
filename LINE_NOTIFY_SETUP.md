# การตั้งค่า LINE Notify สำหรับ Booking System

## ขั้นตอนการขอ LINE Notify Token

### 1. เข้าสู่ระบบ LINE Notify

- ไปที่ https://notify-bot.line.me/
- เข้าสู่ระบบด้วยบัญชี LINE ของคุณ

### 2. สร้าง Access Token

1. คลิกที่ **"My page"** ที่มุมขวาบน
2. เลื่อนลงไปที่ส่วน **"Generate token"**
3. คลิกปุ่ม **"Generate token"**

### 3. กรอกข้อมูล

- **Token name**: ใส่ชื่อที่ต้องการ (เช่น "Boxing Booking System")
- **Select 1-on-1 chat**: เลือกกลุ่มหรือแชทที่ต้องการให้ส่งการแจ้งเตือนไป
    - หากต้องการส่งหาตัวเอง เลือก **"1-on-1 chat with LINE Notify"**
    - หากต้องการส่งไปกลุ่ม เลือกกลุ่มที่ต้องการ (ต้องเพิ่ม LINE Notify เข้ากลุ่มก่อน)

4. คลิก **"Generate token"**

### 4. คัดลอก Token

- **⚠️ สำคัญ**: Token จะแสดงเพียงครั้งเดียว! คัดลอกและเก็บไว้อย่างปลอดภัย
- Token จะมีรูปแบบประมาณ: `abcdefghijklmnopqrstuvwxyz123456789`

## การตั้งค่าในโปรเจค

### 1. สร้างไฟล์ .env.local

สร้างไฟล์ `.env.local` ในโฟลเดอร์รากของโปรเจค (ถ้ายังไม่มี):

```bash
cp .env.local.example .env.local
```

### 2. เพิ่ม Token ในไฟล์ .env.local

เปิดไฟล์ `.env.local` และใส่ TOKEN ที่ได้มา:

```env
LINE_NOTIFY_TOKEN="your_line_notify_token_here"
```

แทนที่ `your_line_notify_token_here` ด้วย Token จริงที่คุณได้จากขั้นตอนที่ 4

### 3. Restart Development Server

หลังจากเพิ่ม Token แล้ว ให้ restart development server:

```bash
npm run dev
# หรือ
pnpm dev
```

## การทดสอบ

### วิธีที่ 1: สร้าง Booking ทดสอบ

สร้าง booking ใหม่ผ่านระบบ จะได้รับการแจ้งเตือนผ่าน LINE ทันที

### วิธีที่ 2: สร้าง API Route สำหรับทดสอบ

สร้างไฟล์ `src/app/api/test-line-notify/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import LineNotifyService from '@/services/LineNotifyService'

export async function GET() {
    const result = await LineNotifyService.testConnection()

    if (result) {
        return NextResponse.json({
            success: true,
            message: 'LINE Notify connection successful!',
        })
    } else {
        return NextResponse.json(
            {
                success: false,
                message: 'Failed to connect to LINE Notify',
            },
            { status: 500 },
        )
    }
}
```

จากนั้นเข้าไปที่: `http://localhost:4000/api/test-line-notify`

## รูปแบบข้อความที่จะได้รับ

เมื่อมีการสร้าง booking ใหม่ คุณจะได้รับข้อความแจ้งเตือนในรูปแบบนี้:

```
🥊 มีการจองใหม่!

📝 รหัสการจอง: ABC123
👥 ลูกค้า: สมชาย ใจดี
📧 อีเมล: somchai@example.com
📱 เบอร์โทร: 081-234-5678
🏋️ ประเภท: ส่วนตัว
👤 เทรนเนอร์: จอห์น โด
📅 วันที่: 5 พฤศจิกายน 2567
⏰ เวลา: 10:00-11:00
👫 จำนวนผู้เข้าร่วม: 1 คน
```

## การปรับแต่งข้อความ

สามารถแก้ไขข้อความที่ส่งได้ที่ไฟล์:
`src/services/LineNotifyService.ts`

ในเมธอด `sendBookingNotification()`

## Troubleshooting

### ไม่ได้รับการแจ้งเตือน

1. ตรวจสอบว่า Token ถูกต้อง
2. ตรวจสอบว่าได้เพิ่ม Token ใน `.env.local` แล้ว
3. ตรวจสอบว่าได้ restart development server แล้ว
4. ดู console logs ใน terminal เพื่อดู error messages

### Error: LINE_NOTIFY_TOKEN is not configured

- ตรวจสอบว่าไฟล์ `.env.local` มีอยู่และอยู่ในโฟลเดอร์รากของโปรเจค
- ตรวจสอบว่าชื่อ environment variable ถูกต้อง: `LINE_NOTIFY_TOKEN`
- Restart development server

### Error 401 (Unauthorized)

- Token ไม่ถูกต้องหรือหมดอายุ
- สร้าง Token ใหม่และอัปเดตใน `.env.local`

### Error 400 (Bad Request)

- ตรวจสอบรูปแบบข้อความ
- ตรวจสอบว่ามีการ encode URL parameters ถูกต้อง

## ข้อควรระวัง

1. **ห้ามเผยแพร่ Token**: อย่า commit ไฟล์ `.env.local` ลง Git
2. **Rate Limit**: LINE Notify มี rate limit อยู่ที่ 1,000 requests ต่อชั่วโมง
3. **Token Security**: เก็บ Token ไว้อย่างปลอดภัย เหมือนกับ password
4. **Production**: ใน production ควรใช้ environment variables ของ hosting provider

## เพิ่มเติม

- [LINE Notify API Document](https://notify-bot.line.me/doc/en/)
- [LINE Notify FAQ](https://help.line.me/line/?contentId=20012097)

---

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อทีมพัฒนา
